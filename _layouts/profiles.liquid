---
layout: page
---
<div class="post">
  <article>
    {% if site.data.profiles %}
      {% for profile_group in site.data.profiles.groups %}
        <h3 class="profile-group">{{ profile_group.label }}</h3>
        {% for profile in profile_group.members %}
          <hr/>
          <div class="profile float-{% if profile.align == 'left' %}left{% else %}right{% endif %}">
            {% if profile.image %}
              {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
              {% if profile.image_circular %}
                {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
              {% else %}
                {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
              {% endif %}
              {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
              {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
            {% endif %}
            {% if profile.more_info %}
              <div class="more-info">{{ profile.more_info }}</div>
            {% endif %}
          </div>

          <div class="clearfix">
            <h5 style="margin-bottom: 0.1rem"><strong>{{ profile.name }}</strong></h5>
            <h6>{{ profile.title }}</h6>
            {% if profile.bio %}
              {% capture profile_content %}{% include_relative {{ profile.bio }} %}{% endcapture %}
              {{ profile_content | markdownify }}
            {% else %}
              {{ content }}
            {% endif %}
          </div>
        {% endfor %}
      {% endfor %}
    {% endif %}
  </article>
</div>
